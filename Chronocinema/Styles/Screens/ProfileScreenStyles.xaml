<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ProfilePictureBorderStyle"  TargetType="Border">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Height" Value="200"/>
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="100"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,40,0,20"/>
    </Style>

    <Style x:Key="ProfileStatisticsStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource GrayBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="15"/>
    </Style>

    <Style x:Key="ProfileStatisticsHeader" TargetType="TextBlock" BasedOn="{StaticResource UserProfileInfoStyle}">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="FontSize" Value="16"/>
    </Style>

    <Style x:Key="ProfileAddMediasCountHeader" TargetType="TextBlock" BasedOn="{StaticResource UserProfileInfoStyle}">
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    
</ResourceDictionary>