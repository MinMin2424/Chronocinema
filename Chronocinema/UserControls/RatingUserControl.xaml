<UserControl x:Class="Chronocinema.UserControls.RatingUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chronocinema.UserControls"
             mc:Ignorable="d" d:DesignWidth="300"
             x:Name="root" Height="70">

    <StackPanel Orientation="Vertical" >
        <!-- Rating Input -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- Slider -->
            <Slider Grid.Column="0"
                    Style="{StaticResource RatingSliderStyle}"
                    Value="{Binding ElementName=root, Path=Rating, Mode=TwoWay}"/>
        </Grid>
        <!-- Rating Display -->
        <Border Grid.Column="1"
                Style="{StaticResource RatingBorderStyle}">
            <TextBlock Text="{Binding ElementName=root, Path=Rating, StringFormat={}{0:F1}/10}"
                       Style="{StaticResource RatingTextBlockStyle}"/>
        </Border>

    </StackPanel>

</UserControl>
